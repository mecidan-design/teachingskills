# Mr TeacherDan's Imaginarium machine

## Resumen de reglas
- No se encontró carpeta `rules/` en la ruta de contenido.

## Categorías detectadas automáticamente
- No se encontraron categorías con subcarpeta `recortadas`.

## Ejecución local
1. (Opcional) exportar `IMAGINARIUM_CONTENT_ROOT` con la carpeta del contenido.
2. `python3 build_data.py`
3. `python3 server.py`
4. Abrir `http://localhost:3000`
